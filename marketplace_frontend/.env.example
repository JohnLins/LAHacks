# Empty keeps local CRA dev proxy behavior.
REACT_APP_API_BASE_URL=

# Use this when the Flask API is hosted separately.
# REACT_APP_API_BASE_URL=https://api.example.com

# Local dev proxy target for npm start.
REACT_APP_API_PROXY_TARGET=http://localhost:5000
